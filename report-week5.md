# 5주차 과제: Agent-Ready 도입 전/후 비교 리포트

## 1. 실험 개요
- **비교 질문**: "환불 수수료 5%를 떼는 기능을 추가하고 싶어. 어느 파일을 고쳐야 하고, 금액 단위는 뭘 쓰고, 로그는 어떻게 남겨야 하는지 계획만 말해줘. 코드는 아직 수정하지 마."
- **실험 환경**: Claude Code (`week05` 해독 실험실 — 규칙 파일(`CLAUDE.md`, `GOLDEN_RULES.md`, `.agent-rules.md`, `.claude/commands/payment-feature.md`) 적용 전 vs 적용 후)
- **비교 방식**: 같은 질문을 도입 전 2회, 도입 후 2회 반복 실행하여 답변의 일관성(Variance)까지 함께 관찰

---

## 2. 도입 전 답변 (규칙 파일 없음)
> `/clear` 상태에서 규칙 파일이 없을 때, 같은 질문을 두 번 반복한 답변

**공통적으로 관찰된 점**: 규칙 파일이 없으니 AI가 코드를 직접 뒤져서(`src/billing/refund.js`, `src/payments/refund.js`, `src/old/legacy.js`, `package.json`, 테스트 파일 등) 어느 파일이 실제로 쓰이는지 스스로 추론해야 했습니다. 두 번의 답변 모두 결국 `src/payments/refund.js`를 정답으로 찾아내긴 했지만, 과정과 세부 제안 내용이 매번 달랐습니다.

```text
[1차 답변 요약]
- 세 파일(src/payments, src/billing, src/old)을 표로 나열하며 직접 비교/조사
- "5%를 뗀다"는 게 기존 0.1% 수수료율을 대체하는 것인지, 위에 추가로 얹는 것인지
  확신하지 못해 사용자에게 먼저 확인을 요청함
- feeRate를 로그에 추가할지, 매직넘버 대신 상수로 뺄지 등을 "고려 가능"으로만
  언급하고 결정을 유보함

[2차 답변 요약]
- 동일한 질문인데도 조사 과정(검색/읽은 파일 수)과 설명 순서가 달라짐
- 역시 수수료율 교체 여부, 상수 분리 여부, 로그 필드 추가 여부를 사용자에게
  재확인 요청 — 매번 "이대로 진행해도 될까요?"로 끝나며 결론을 내리지 못함
```

**핵심 문제**: 규칙이 코드로 존재하지 않으니, AI가 매번 처음부터 코드베이스를 다시 조사해야 했고, 애매한 지점(수수료율 교체 vs 누적, 로그 필드 구성)에서 확신을 갖지 못해 질문으로 되돌려 보냈습니다. 두 번의 답변이 최종 결론은 같았지만 조사 경로와 부가 제안이 서로 달라, **일관성 없는(non-deterministic) 계획**이 나왔습니다.

---

## 3. 도입 후 답변 (`CLAUDE.md` / `GOLDEN_RULES.md` / `.agent-rules.md` / `/payment-feature` 슬래시 명령 적용 후)
> 규칙 파일과 워크플로우 슬래시 명령을 심은 뒤, 동일한 질문을 두 번 반복한 답변

```text
[3차 답변 요약]
- 질문을 받자마자 곧바로 "관련 워크플로우 스킬을 먼저 확인하겠습니다"라며
  Skill(/payment-feature)를 스스로 로드
- CLAUDE.md·GOLDEN_RULES.md·docs/payment-rules.md·.agent-rules.md 기준에 맞춰
  src/payments/refund.js만 확인하고, src/billing/·src/old/는 언급만 하고 건드리지 않음
- 금액 단위(amountMinor, 전 단위 정수), 로깅(logPayment()만 사용),
  수수료 계산식(Math.floor(amountMinor * 0.05))까지 망설임 없이 규칙 문서 기준으로 답변
- npm test까지 직접 실행해 통과 여부를 확인하고 보고

[4차 답변 요약]
- 3차와 완전히 동일한 절차(/payment-feature 스킬 로드 → 규칙 문서 대조 → 검증)로 진행
- 파일 경로, 금액 단위, 로그 함수, 수수료 계산식 등 핵심 답변 내용이 3차와 100% 일치
- 규칙에 없는 추가 요구(수수료율 변경, 한도 설정 등)가 있을 경우에만
  "그 경우엔 별도 계획을 세우겠다"고 범위를 명확히 구분해 답변
```

**핵심 변화**: 규칙 파일과 슬래시 명령이 생기자 AI는 더 이상 코드를 처음부터 탐정처럼 조사하지 않고, 규칙 문서를 근거로 곧바로 정답 경로(`src/payments/`), 금액 단위(minor unit), 로그 함수(`logPayment()`)를 제시했습니다. 두 번의 답변이 조사 절차와 결론 모두 **동일**했습니다.

---

## 4. 나의 관찰 3줄 이상 (핵심 관찰 기록)
1. **결정론적 수렴**: 도입 전에는 같은 질문을 두 번 던져도 조사 경로와 부가 제안이 매번 달랐고, 애매한 지점(수수료율 교체 vs 누적)에서 AI가 확신 없이 사용자에게 되물었습니다. 반면 도입 후에는 두 번의 답변이 조사 절차·결론·세부 수치까지 완전히 동일하게 수렴했습니다 — 규칙 파일이 있고 없고의 차이가 답변의 일관성(Variance)을 눈에 띄게 줄였습니다.
2. **탐정 모드 → 참조 모드**: 도입 전에는 AI가 `package.json`, 테스트 파일, 세 개의 후보 파일을 매번 직접 열어보며 "어느 것이 진짜인지" 추론하는 데 시간을 썼습니다. 도입 후에는 그 조사 과정 없이 곧바로 `/payment-feature` 슬래시 명령을 스스로 호출해 규칙 문서(CLAUDE.md, GOLDEN_RULES.md, docs/payment-rules.md)를 근거로 답했습니다. 즉, 암묵지가 파일로 코드화되자 AI의 행동이 "코드를 뒤져서 추측"에서 "규칙을 참조해서 확인"으로 바뀌었습니다.
3. **애매함 처리 방식의 변화**: 도입 전에는 애매한 부분(수수료 계산 방식, 로그 필드 구성)을 AI가 스스로 판단하지 못하고 매번 사용자에게 되묻는 "확인 요청형" 답변으로 끝났습니다. 도입 후에는 `docs/payment-rules.md`에 계산 규칙(`floor(amountMinor * 0.05)`)이 명문화되어 있어 같은 질문에도 재확인 없이 바로 결론을 내렸고, 규칙 범위 밖의 요구가 있을 때만 선택지를 나누어 제시했습니다 — 컨텍스트 엔지니어링이 AI의 "질문 남발"을 줄이고 실행 가능한 답변을 늘렸습니다.
4. **워크플로우 자동화의 부가 효과**: `.claude/commands/payment-feature.md` 슬래시 명령을 만들어두자, 사용자가 명시적으로 명령을 부르지 않아도 AI가 "결제 기능 추가 요청이니 관련 워크플로우 스킬을 먼저 확인하겠습니다"라며 자발적으로 규칙을 먼저 로드했습니다. 즉 도구(Tooling)를 다리로 놓아두면 AI가 스스로 그 다리를 건너는 모습을 확인할 수 있었습니다.

