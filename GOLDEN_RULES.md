# GOLDEN RULES (절대 위반 금지)

1. **과도한 복잡화 금지**: React/Vue 등 거대 프레임워크나 복잡한 빌드 도구를 도입하지 않고 바닐라 웹 기술을 유지한다.
2. **스타일 일관성 유지**: 임의의 원색(쨍한 빨강, 파랑 등)을 직접 하드코딩하지 말고, `styles.css`에 정의된 파스텔 톤 팔레트를 사용한다.
3. **데이터 보존**: `localStorage` 구조 변경 시 기존 사용자 일기 데이터가 유실되지 않도록 기본값 예외 처리를 반드시 둔다.
4. **한글 커밋 메시지**: 커밋 메시지는 `CONTRIBUTING.md` 규격에 맞춰 한국어로 간결하게 작성한다 (`feat:`, `fix:` 등).
5. **독립성 유지**: 다른 페이지(`hello.html` 등)의 스타일을 건드려 `feel.html`이 깨지지 않도록 클래스 네임스페이스를 준수한다.