# School_VibeCoding — 감정 일기 프로젝트

## 1. 프로젝트 개요
- 브라우저 기반의 파스텔 톤 감정 일기 및 기록 웹 애플리케이션

## 2. 파일 구조 및 역할
- `feel.html`: 감정 일기 메인 페이지 (기분 선택, 일기 작성, 로컬 저장 목록)
- `styles.css`: 디자인 토큰(파스텔 테마 색상, CSS 변수), 반응형 레이아웃 및 애니메이션
- `script.js`: 공통 유틸리티 및 인터랙션 스크립트
- `hello.html`, `guestbook.html`, `expense.html`: 부가 기능 페이지

## 3. 핵심 아키텍처 규칙
- 데이터 저장은 브라우저 `localStorage`를 사용하며, 키 이름은 일관되게 유지한다.
- 스타일 수정 시 인라인 스타일을 지양하고 `styles.css`의 CSS 변수(`--card-border`, `--theme-color` 등)를 우선 활용한다.
- 불필요하게 무거운 외부 라이브러리를 추가하지 않고 바닐라 JS/CSS를 유지한다.
- 절대 위반 금지 규칙은 `@GOLDEN_RULES.md`를 참고할 것.

## 4. 작업 후 검증 절차
- HTML/JS 수정 후 브라우저에서 직접 열어 콘솔 오류가 없는지 확인한다.
- 데이터 추가/삭제 시 `localStorage`에 올바른 JSON 형식으로 저장되는지 검증한다.